enum LocalStorageKey {
  token,
  userId,
  cart,
  isGuestCheckout,
}

enum IndicatorSize {
  tiny,
  normal,
  full,
}

enum ProductsViewType {
  list,
  grid,
}

enum Gender {
  male,
  female,
  others,
}